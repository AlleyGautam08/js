// parameter destructuring
// object react

const person = {
     firstname: "Alley",
     gender: "Female",
}
function printdetails(obj){
     console.log(obj.firstname);
     console.log(obj.gender);
     console.log(obj.age);
}
printdetails(person);